# Use the latest official Ubuntu image as base
FROM ubuntu:latest

# Update and upgrade APT packages
RUN apt-get update && apt-get upgrade -y

# Default command to echo "Hello, World!"
CMD echo "Hello, World!"
